# Extract port from Host header for development
map $http_host $port_in_host {
    ~^[^\:]+:(?<p>\d+)$ $p;
    default 80;
}

# Detect HTTPS from X-Forwarded-Proto (Coolify/reverse proxy)
map $http_x_forwarded_proto $https_value {
    default "";
    https "on";
}

server {
    listen 80;
    server_name _;
    
    root /var/www/html/public;
    index index.php index.html;
    
    # Maximum upload size
    client_max_body_size 100M;
    
    # Logging
    access_log /var/log/nginx/moodle-access.log;
    error_log /var/log/nginx/moodle-error.log;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Hide all dot files but allow "Well-Known URIs" as per RFC 5785 (Moodle docs)
    location ~ /\.(?!well-known).* {
        return 404;
    }
    
    # Don't allow direct access to various internal files (Moodle docs - MDL-69333)
    location ~ (/vendor/|/node_modules/|composer\.json|/readme|/README|readme\.txt|/upgrade\.txt|/UPGRADING\.md|db/install\.xml|/fixtures/|/behat/|phpunit\.xml|\.lock|environment\.xml) {
        deny all;
        return 404;
    }
    
    # Additional security for .env and .git
    location ~ (/\.git|\.env$) {
        deny all;
        return 404;
    }
    
    # Moodle 4.5+ routing engine
    # Route to r.php if file/directory doesn't exist
    location / {
        try_files $uri $uri/ /r.php;
    }
    
    # Strip /public/ prefix if present (legacy URLs)
    location ^~ /public/ {
        rewrite ^/public/(.*)$ /$1 permanent;
    }
    
    # Deny access to moodledata directory
    location ^~ /moodledata/ {
        internal;
        deny all;
    }
    
    # PHP-FPM processing
    location ~ \.php(/|$) {
        # Split the path info based on URI.
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        # Note: Store the original path_info. It will be wiped out in a moment by try_files.
        set $path_info $fastcgi_path_info;
        # Look for the php file. If not found then jump to @routed.
        try_files $fastcgi_script_name $fastcgi_script_name/;
        # File was found – pass to fastcgi.
        fastcgi_pass   127.0.0.1:9000;
        include        fastcgi_params;
        # Re-apply the path_info after including fastcgi_params.
        fastcgi_param PATH_INFO $path_info;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
        
        # Override host/port params to include port number for Moodle
        fastcgi_param HTTP_HOST $http_host;
        fastcgi_param SERVER_NAME $http_host;
        fastcgi_param SERVER_PORT $port_in_host;
        
        # Pass X-Forwarded headers to PHP (for reverse proxy detection)
        fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
        fastcgi_param HTTP_X_FORWARDED_HOST $http_x_forwarded_host;
        fastcgi_param HTTP_X_FORWARDED_PORT $http_x_forwarded_port;
        
        # Set HTTPS=on when behind HTTPS proxy (for PHP/Moodle detection)
        fastcgi_param HTTPS $https_value;
        
        # Timeouts
        fastcgi_read_timeout 300;
        fastcgi_send_timeout 300;
        fastcgi_connect_timeout 60;
        
        # Buffer settings
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
    }
}
